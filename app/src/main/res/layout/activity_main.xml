<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:padding="16dp">

    <!-- Titlu -->
    <TextView
        android:id="@+id/textViewClient"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Client"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:layout_marginBottom="16dp" />

    <!-- Câmp pentru introducerea cuvântului -->
    <EditText
        android:id="@+id/editTextWord"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Introduceți un cuvânt"
        android:layout_marginBottom="16dp"
        android:padding="8dp"
        android:backgroundTint="#000000" />

    <!-- Text "Definiție" -->
    <TextView
        android:id="@+id/textViewDefinitionTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Definiție"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:layout_marginBottom="8dp" />

    <!-- Câmp pentru afișarea definiției -->
    <TextView
        android:id="@+id/textViewDefinition"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Definiția va apărea aici."
        android:textSize="16sp"
        android:textColor="#555555"
        android:padding="8dp"
        android:background="#EEEEEE"
        android:layout_marginBottom="16dp" />

</LinearLayout>
